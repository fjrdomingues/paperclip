# 2026-03-28 Daily Notes

## Timeline

- **~11:23** Lara Correia (+351926486594) received outreach. She replied at 11:25 asking to be removed ("retirem o meu número da vossa lista de distribuição, eu nem consultora imobiliária sou"). Bot immediately replied with generic handoff "Um dos nossos consultores vai contactá-lo em breve" — ignoring her opt-out completely.
- **~12:03** LLM classifier correctly identified her as opted_out (confidence 0.95).
- **~12:30** Fábio sent screenshot of the conversation showing the problem.
- **~12:34** Investigated and found root cause in house-remodel-ai repo:
  1. `prompts/agent/whatsapp_assistant.md` has NO opt-out handling instructions
  2. `src/agent/agent.ts:121` always overrides AI response with generic handoff message when notify_admin is called
  3. The auto-reply (SID SMeecca317d909a8488fa7e64862030808) was sent by the webhook-based bot, not our pipeline
- **~12:34** Messaged Fábio with full root cause analysis
- **~12:35** Created WIN-168 (CRITICAL): Bot ignores opt-out requests. Assigned to Engineering Manager. Fix is two files: add opt-out instructions to prompt + don't override AI text in agent.ts.

## Key Decisions
- The house-remodel-ai webhook bot is the source of the problem, not the outreach pipeline
- Fix approach: prompt instructions + code fix (don't override AI response when it already has text)

## Afternoon Session (~18:15)
- **WIN-166** (Make bot less informal): Changed prompt Language section — "você" instead of "tu". Done.
- **WIN-167** (Investigate escalation mechanism): Full investigation. Bot hardcoded "um consultor vai contactá-lo" on every handoff, breaking the real-person illusion. Fix: removed hardcoded override in agent.ts, updated prompt to never mention team/consultores. Done.
- **WIN-168** (Bot ignores opt-out): Added opt-out handling to prompt (confirm removal + apologize + stop). Combined with WIN-167 fix — AI response now passes through instead of being overridden. Done.
- All three fixes in commit `71111d0` in house-remodel-ai repo
- Build passes clean. Server not running locally — needs deploy/restart for agent.ts change. Prompt changes are live (read from file at runtime).

## Open Items
- Server restart needed to deploy agent.ts handoff fix (prompt changes are already live)
- Need to verify fix works on production after deploy (test with opt-out message AND regular handoff)
