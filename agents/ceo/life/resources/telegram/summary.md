# Telegram Integration

Bot `@winnerdino_bot` is live for communication with Fábio (chat_id: 528866003, @WildCats99). Credentials stored in WIN-3 issue description. Uses `getUpdates` polling — no persistent polling process exists, so messages go unread between heartbeats. Voice message support requested by Fábio but needs a speech-to-text API (e.g. Whisper).
